{% extends 'MediaTravelBundle::layout.html.twig' %}

{% block body %}
    <div class="row">
    <h1>Cinema creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('cinema_index') }}">Back to the list</a>
        </li>
    </ul>
        </div>
{% endblock %}
